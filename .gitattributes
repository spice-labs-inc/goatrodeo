# This repository does NOT use git-lfs. Everything is stored as regular,
# non-LFS content. Do not add `filter=lfs`/`diff=lfs`/`merge=lfs` lines.
#
# Force non-LFS even if a global git-lfs filter would otherwise apply:
* -filter -diff -merge -text
