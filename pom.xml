<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.spicelabs</groupId>
  <artifactId>goatrodeo_3</artifactId>
  <version>0.0.0-SNAPSHOT</version>
  <packaging>jar</packaging>
  <name>GoatRodeo</name>
  <description>Static analysis toolkit for unpacking artifacts</description>
  <url>https://github.com/spice-labs-inc/goatrodeo</url>

  <!--
    Compile-from-source Maven build, ported from build.sbt. This is a
    proof-of-concept living alongside the sbt build; `maven/pom.xml` remains
    the publish-only shell used by the release workflow.

    Quick start:
      mvn -DskipTests compile     # fast: verify it compiles
      mvn test                    # runs the suite (see PreflightSuite for
                                  # the test-data / git-lfs prerequisites)
  -->

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <scala.version>3.8.3</scala.version>
    <maven.compiler.release>17</maven.compiler.release>

    <!-- Library versions, aligned with build.sbt -->
    <scala-xml.version>2.3.0</scala-xml.version>
    <asm.version>9.8</asm.version>
    <bcel.version>6.11.0</bcel.version>
    <scopt.version>4.1.0</scopt.version>
    <commons-io.version>2.18.0</commons-io.version>
    <borer.version>1.14.1</borer.version>
    <isofilereader.version>0.6.1</isofilereader.version>
    <json4s.version>4.0.7</json4s.version>
    <commons-compress.version>1.28.0</commons-compress.version>
    <logback.version>1.5.15</logback.version>
    <scala-parallel-collections.version>1.2.0</scala-parallel-collections.version>
    <scala-logging.version>3.9.4</scala-logging.version>
    <tika.version>3.2.3</tika.version>
    <packageurl.version>1.5.0</packageurl.version>
    <cilantro.version>0.1.17</cilantro.version>
    <coordinates.version>1.1.0</coordinates.version>
    <baharat.version>0.1.1</baharat.version>
    <annatto.version>0.1.0</annatto.version>
    <saffron.version>0.3.1</saffron.version>
    <bouncycastle.version>1.80</bouncycastle.version>
    <scala-nameof.version>5.0.0</scala-nameof.version>
    <munit.version>0.7.29</munit.version>
    <scalacheck.version>1.18.1</scalacheck.version>
    <jackson.version>2.18.1</jackson.version>
    <json-schema-validator.version>1.5.4</json-schema-validator.version>

    <!-- Test tuning (mirrors build.sbt) -->
    <!-- Test forking/parallelism tuning.
         TEST_THREAD_CNT=anything switches to a single non-forked JVM for CI.
         TEST_FORK=anything forces forking.
         GC warnings are redirected to a file because the G1GC GCLocker
         warning is written to the native console stream and corrupts Surefire's
         IPC channel when tests fork; this made ADGTests look like an
         InterruptedException failure. -->
    <test.forkCount>4</test.forkCount>
    <test.reuseForks>true</test.reuseForks>
    <test.parallel>none</test.parallel>
    <test.threadCount>4</test.threadCount>
    <test.argLine>-Xmx8G -Duser.timezone=GMT -Xlog:gc+alloc=warning:file=${project.build.directory}/gc-%p.log</test.argLine>
  </properties>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.html</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <repositories>
    <repository>
      <id>central</id>
      <url>https://repo1.maven.org/maven2/</url>
    </repository>
    <repository>
      <id>ow2</id>
      <url>https://repository.ow2.org/nexus/content/repositories/public/</url>
    </repository>
  </repositories>

  <dependencies>
    <!-- Scala 3 standard library (implicit under sbt, explicit under Maven).
         Pinned to scala.version. -->
    <dependency>
      <groupId>org.scala-lang</groupId>
      <artifactId>scala3-library_3</artifactId>
      <version>${scala.version}</version>
    </dependency>

    <!-- `%%` deps from build.sbt carry the _3 suffix here -->
    <dependency>
      <groupId>org.scala-lang.modules</groupId>
      <artifactId>scala-xml_3</artifactId>
      <version>${scala-xml.version}</version>
    </dependency>
    <dependency>
      <groupId>org.ow2.asm</groupId>
      <artifactId>asm</artifactId>
      <version>${asm.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.bcel</groupId>
      <artifactId>bcel</artifactId>
      <version>${bcel.version}</version>
    </dependency>
    <dependency>
      <groupId>com.github.scopt</groupId>
      <artifactId>scopt_3</artifactId>
      <version>${scopt.version}</version>
    </dependency>
    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <version>${commons-io.version}</version>
    </dependency>
    <dependency>
      <groupId>io.bullet</groupId>
      <artifactId>borer-derivation_3</artifactId>
      <version>${borer.version}</version>
    </dependency>
    <dependency>
      <groupId>com.palantir.isofilereader</groupId>
      <artifactId>isofilereader</artifactId>
      <version>${isofilereader.version}</version>
    </dependency>
    <dependency>
      <groupId>org.json4s</groupId>
      <artifactId>json4s-native_3</artifactId>
      <version>${json4s.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-compress</artifactId>
      <version>${commons-compress.version}</version>
    </dependency>
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
      <version>${logback.version}</version>
    </dependency>
    <dependency>
      <groupId>org.scala-lang.modules</groupId>
      <artifactId>scala-parallel-collections_3</artifactId>
      <version>${scala-parallel-collections.version}</version>
    </dependency>
    <dependency>
      <groupId>com.typesafe.scala-logging</groupId>
      <artifactId>scala-logging_3</artifactId>
      <version>${scala-logging.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.tika</groupId>
      <artifactId>tika-core</artifactId>
      <version>${tika.version}</version>
    </dependency>
    <dependency>
      <groupId>com.github.package-url</groupId>
      <artifactId>packageurl-java</artifactId>
      <version>${packageurl.version}</version>
    </dependency>

    <!-- Spice Labs -->
    <dependency>
      <groupId>io.spicelabs</groupId>
      <artifactId>cilantro_3</artifactId>
      <version>${cilantro.version}</version>
    </dependency>
    <dependency>
      <groupId>io.spicelabs</groupId>
      <artifactId>coordinates</artifactId>
      <version>${coordinates.version}</version>
    </dependency>
    <dependency>
      <groupId>io.spicelabs</groupId>
      <artifactId>baharat</artifactId>
      <version>${baharat.version}</version>
    </dependency>
    <dependency>
      <groupId>io.spicelabs</groupId>
      <artifactId>annatto</artifactId>
      <version>${annatto.version}</version>
    </dependency>
    <dependency>
      <groupId>io.spicelabs</groupId>
      <artifactId>saffron</artifactId>
      <version>${saffron.version}</version>
    </dependency>

    <!-- BouncyCastle -->
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcprov-jdk18on</artifactId>
      <version>${bouncycastle.version}</version>
    </dependency>
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcpkix-jdk18on</artifactId>
      <version>${bouncycastle.version}</version>
    </dependency>
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcpg-jdk18on</artifactId>
      <version>${bouncycastle.version}</version>
    </dependency>
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcutil-jdk18on</artifactId>
      <version>${bouncycastle.version}</version>
    </dependency>

    <!-- Compile-time only macro (`% "provided"` in sbt) -->
    <dependency>
      <groupId>com.github.dwickern</groupId>
      <artifactId>scala-nameof_3</artifactId>
      <version>${scala-nameof.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>org.scalameta</groupId>
      <artifactId>munit_3</artifactId>
      <version>${munit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.scalameta</groupId>
      <artifactId>munit-scalacheck_3</artifactId>
      <version>${munit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.scalacheck</groupId>
      <artifactId>scalacheck_3</artifactId>
      <version>${scalacheck.version}</version>
      <scope>test</scope>
    </dependency>

    <!-- JSON parsing/validation used by CBOM tests -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.networknt</groupId>
      <artifactId>json-schema-validator</artifactId>
      <version>${json-schema-validator.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <sourceDirectory>src/main/scala</sourceDirectory>
    <testSourceDirectory>src/test/scala</testSourceDirectory>

    <plugins>
      <!-- Read the current git commit for BuildInfo -->
      <plugin>
        <groupId>io.github.git-commit-id</groupId>
        <artifactId>git-commit-id-maven-plugin</artifactId>
        <version>9.0.1</version>
        <executions>
          <execution>
            <id>get-git-info</id>
            <phase>initialize</phase>
            <goals>
              <goal>revision</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <failOnNoGitDirectory>false</failOnNoGitDirectory>
          <failOnUnableToExtractRepoInfo>false</failOnUnableToExtractRepoInfo>
          <generateGitPropertiesFile>false</generateGitPropertiesFile>
        </configuration>
      </plugin>

      <!-- Generate hellogoat.BuildInfo from the template (replaces sbt-buildinfo) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution>
            <id>generate-buildinfo</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/generated-sources/scala/hellogoat</outputDirectory>
              <resources>
                <resource>
                  <directory>${project.basedir}/maven/buildinfo</directory>
                  <includes>
                    <include>BuildInfo.scala</include>
                  </includes>
                  <filtering>true</filtering>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Register the generated BuildInfo dir as a source root -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <id>add-generated-source</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>add-source</goal>
            </goals>
            <configuration>
              <sources>
                <source>${project.build.directory}/generated-sources/scala</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Fetch external test fixtures and git-lfs content before tests run.
           Mirrors the Tests.Setup hook from build.sbt. -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <id>fetch-test-data</id>
            <phase>generate-test-resources</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>bash</executable>
              <arguments>
                <argument>${project.basedir}/bin/fetch-test-data.sh</argument>
              </arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Scala 3 compiler -->
      <plugin>
        <groupId>net.alchim31.maven</groupId>
        <artifactId>scala-maven-plugin</artifactId>
        <version>4.9.2</version>
        <configuration>
          <scalaVersion>${scala.version}</scalaVersion>
          <args>
            <arg>-deprecation</arg>
            <arg>-unchecked</arg>
            <arg>-feature</arg>
            <arg>-Wunused:imports</arg>
            <arg>-Yexplicit-nulls</arg>
            <arg>-no-indent</arg>
            <arg>-release</arg>
            <arg>17</arg>
          </args>
        </configuration>
        <executions>
          <execution>
            <id>scala-compile</id>
            <phase>compile</phase>
            <goals>
              <goal>compile</goal>
            </goals>
          </execution>
          <execution>
            <id>scala-test-compile</id>
            <phase>test-compile</phase>
            <goals>
              <goal>testCompile</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- Run munit via its JUnit4 runner (munit.MUnitRunner) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.2</version>
        <configuration>
          <includes>
            <include>**/*Suite.class</include>
            <include>**/*Test.class</include>
            <include>**/*Tests.class</include>
            <include>**/*Testing.class</include>
            <include>**/*Probe.class</include>
          </includes>
          <excludes>
            <!-- These assert on the packaged fat jar, which is built in the
                 `package` phase (after `test`). Run them post-package instead. -->
            <exclude>**/FatJarContentsTest.class</exclude>
            <exclude>**/FatJarExecutionTest.class</exclude>
          </excludes>
          <forkCount>${test.forkCount}</forkCount>
          <reuseForks>${test.reuseForks}</reuseForks>
          <parallel>${test.parallel}</parallel>
          <threadCount>${test.threadCount}</threadCount>
          <redirectTestOutputToFile>true</redirectTestOutputToFile>
          <argLine>${test.argLine}</argLine>
        </configuration>
      </plugin>

      <!-- Fat jar (replaces sbt-assembly); strips signature files like the
           sbt assemblyMergeStrategy did -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <shadedArtifactAttached>true</shadedArtifactAttached>
              <shadedClassifierName>fat</shadedClassifierName>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                    <exclude>META-INF/*.EC</exclude>
                    <exclude>META-INF/SIG-*</exclude>
                  </excludes>
                </filter>
              </filters>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>io.spicelabs.goatrodeo.Howdy</mainClass>
                </transformer>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Copy the shaded fat jar to the path the sbt-origin fat-jar tests
           expect, so they can run unchanged under Maven. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>3.1.0</version>
        <executions>
          <execution>
            <id>stage-fat-jar-for-tests</id>
            <phase>package</phase>
            <configuration>
              <target>
                <mkdir dir="${project.build.directory}/scala-3.8.3"/>
                <copy
                  file="${project.build.directory}/goatrodeo_3-${project.version}-fat.jar"
                  tofile="${project.build.directory}/scala-3.8.3/goatrodeo-${project.version}-fat.jar"
                  overwrite="true"/>
              </target>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- Fat-jar tests run after the package phase builds the fat jar.
           Mirrors build.sbt's Test/test dependency on verifyJarContents/testFatJar. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>3.5.2</version>
        <executions>
          <execution>
            <id>run-fat-jar-tests</id>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <includes>
            <include>**/FatJarContentsTest.class</include>
            <include>**/FatJarExecutionTest.class</include>
          </includes>
          <argLine>${test.argLine}</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <!-- Environment-based test tuning, matching build.sbt:
       - TEST_THREAD_CNT set  -> CI mode: no forking, no parallelism
       - TEST_THREAD_CNT unset -> dev mode: fork and run classes in parallel
       - TEST_FORK set         -> force forking even when TEST_THREAD_CNT is set -->
  <profiles>
    <profile>
      <id>ci</id>
      <activation>
        <property>
          <name>env.TEST_THREAD_CNT</name>
        </property>
      </activation>
      <properties>
        <test.forkCount>0</test.forkCount>
        <test.parallel>none</test.parallel>
      </properties>
    </profile>
    <profile>
      <id>force-fork</id>
      <activation>
        <property>
          <name>env.TEST_FORK</name>
        </property>
      </activation>
      <properties>
        <test.forkCount>4</test.forkCount>
      </properties>
    </profile>
  </profiles>
</project>
